# @vibecook/ghosttead-darwin-arm64

The `ghosttead` daemon binary for macOS on Apple silicon. This package is
installed automatically as an optional dependency of
[`@vibecook/ghosttead`](https://www.npmjs.com/package/@vibecook/ghosttead) —
depend on that package instead.
