# @vibecook/ghosttead-win32-x64

The `ghosttead` daemon binary for Windows on x64. This package is installed
automatically as an optional dependency of
[`@vibecook/ghosttead`](https://www.npmjs.com/package/@vibecook/ghosttead) —
depend on that package instead.
