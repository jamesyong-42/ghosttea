diff --git a/include/ghostty/vt/formatter.h b/include/ghostty/vt/formatter.h
index 5cdcd11a3a7aed8aa73f9c2104ba10df0391a503..6da16fe6133966fbc3459f1dbf755bd3df03ff2d 100644
--- a/include/ghostty/vt/formatter.h
+++ b/include/ghostty/vt/formatter.h
@@ -139,0 +140,25 @@ GHOSTTY_API GhosttyResult ghostty_formatter_terminal_new(
+/**
+ * Create a formatter that emits terminal state without screen content.
+ *
+ * This is the state-only counterpart to ghostty_formatter_terminal_new().
+ * The options and extras have the same meaning, except `selection` is ignored
+ * because no cells are formatted. For VT output this exposes the formatter's
+ * state restoration sequences without changing the ABI of the by-value
+ * GhosttyFormatterTerminalOptions structure.
+ *
+ * The terminal must outlive the formatter.
+ *
+ * @param allocator Pointer to allocator, or NULL to use the default allocator
+ * @param formatter Pointer to store the created formatter handle
+ * @param terminal The terminal to format (must not be NULL)
+ * @param options Formatting options
+ * @return GHOSTTY_SUCCESS on success, or an error code on failure
+ *
+ * @ingroup formatter
+ */
+GHOSTTY_API GhosttyResult ghostty_formatter_terminal_state_new(
+    const GhosttyAllocator* allocator,
+    GhosttyFormatter* formatter,
+    GhosttyTerminal terminal,
+    GhosttyFormatterTerminalOptions options);
+
diff --git a/include/ghostty/vt/grid_ref.h b/include/ghostty/vt/grid_ref.h
index c43791dc238cf09b8c8488ef10c044e67eb44440..c463d9bd610947739cbee464609082d6ef116f69 100644
--- a/include/ghostty/vt/grid_ref.h
+++ b/include/ghostty/vt/grid_ref.h
@@ -113,0 +114,20 @@ typedef struct {
+/** Semantic identity kind for a hyperlink. */
+typedef enum GHOSTTY_ENUM_TYPED {
+  /** The cell has no hyperlink. */
+  GHOSTTY_HYPERLINK_IDENTITY_NONE = 0,
+  /** The original OSC 8 `id=` bytes are returned. */
+  GHOSTTY_HYPERLINK_IDENTITY_EXPLICIT = 1,
+  /** `implicit_token` is the link's cross-page equality token. */
+  GHOSTTY_HYPERLINK_IDENTITY_IMPLICIT = 2,
+  GHOSTTY_HYPERLINK_IDENTITY_MAX_VALUE = GHOSTTY_ENUM_MAX_VALUE,
+} GhosttyHyperlinkIdentityTag;
+
+/** Semantic hyperlink identity, never the page-local hyperlink-set index. */
+typedef struct {
+  /** Size of this struct in bytes. Must be initialized with GHOSTTY_INIT_SIZED. */
+  size_t size;
+  GhosttyHyperlinkIdentityTag tag;
+  /** Meaningful only when tag is GHOSTTY_HYPERLINK_IDENTITY_IMPLICIT. */
+  uint64_t implicit_token;
+} GhosttyHyperlinkIdentity;
+
@@ -192,0 +213,20 @@ GHOSTTY_API GhosttyResult ghostty_grid_ref_hyperlink_uri(
+/**
+ * Get the semantic identity of the hyperlink at a grid reference.
+ *
+ * Explicit identities return the original OSC 8 `id=` bytes through `buf`.
+ * Implicit identities return a token whose equality is stable across pages
+ * within the owning screen. The internal page-local hyperlink-set index is
+ * never exposed. Hyperlink equality is the pair of this identity and the URI.
+ *
+ * Pass NULL/zero for `buf` to query the explicit identity length. When the
+ * buffer is too small, returns GHOSTTY_OUT_OF_SPACE and writes the required
+ * byte count to `out_len`. Cells without a hyperlink return success with tag
+ * NONE and length zero.
+ */
+GHOSTTY_API GhosttyResult ghostty_grid_ref_hyperlink_identity(
+    const GhosttyGridRef *ref,
+    uint8_t *buf,
+    size_t buf_len,
+    size_t *out_len,
+    GhosttyHyperlinkIdentity *out_identity);
+
diff --git a/include/ghostty/vt/terminal.h b/include/ghostty/vt/terminal.h
index 26ee0c0b8670b4a7c81a4840480063b03bb320a7..28a54fd337d03c552cd8723c323a6460e9953dc3 100644
--- a/include/ghostty/vt/terminal.h
+++ b/include/ghostty/vt/terminal.h
@@ -319,0 +320,96 @@ typedef enum GHOSTTY_ENUM_TYPED {
+/** Character set designation stored by a terminal screen. */
+typedef enum GHOSTTY_ENUM_TYPED {
+  GHOSTTY_TERMINAL_CHARSET_UTF8 = 0,
+  GHOSTTY_TERMINAL_CHARSET_ASCII = 1,
+  GHOSTTY_TERMINAL_CHARSET_BRITISH = 2,
+  GHOSTTY_TERMINAL_CHARSET_DEC_SPECIAL = 3,
+  GHOSTTY_TERMINAL_CHARSET_MAX_VALUE = GHOSTTY_ENUM_MAX_VALUE,
+} GhosttyTerminalCharset;
+
+/** Character set slot used for GL, GR, and single-shift state. */
+typedef enum GHOSTTY_ENUM_TYPED {
+  GHOSTTY_TERMINAL_CHARSET_SLOT_G0 = 0,
+  GHOSTTY_TERMINAL_CHARSET_SLOT_G1 = 1,
+  GHOSTTY_TERMINAL_CHARSET_SLOT_G2 = 2,
+  GHOSTTY_TERMINAL_CHARSET_SLOT_G3 = 3,
+  GHOSTTY_TERMINAL_CHARSET_SLOT_MAX_VALUE = GHOSTTY_ENUM_MAX_VALUE,
+} GhosttyTerminalCharsetSlot;
+
+/** Complete character set state for one screen. */
+typedef struct {
+  GhosttyTerminalCharset g0;
+  GhosttyTerminalCharset g1;
+  GhosttyTerminalCharset g2;
+  GhosttyTerminalCharset g3;
+  GhosttyTerminalCharsetSlot gl;
+  GhosttyTerminalCharsetSlot gr;
+  bool has_single_shift;
+  GhosttyTerminalCharsetSlot single_shift;
+} GhosttyTerminalCharsetState;
+
+/** A populated DECSC saved-cursor slot for one terminal screen. */
+typedef struct {
+  /** Size of this struct in bytes. Must be initialized with GHOSTTY_INIT_SIZED. */
+  size_t size;
+  uint16_t x;
+  uint16_t y;
+  GhosttyStyle style;
+  bool protected_cell;
+  bool pending_wrap;
+  bool origin;
+  GhosttyTerminalCharsetState charset;
+} GhosttyTerminalSavedCursor;
+
+/** Most recently selected protected-character mechanism for a screen. */
+typedef enum GHOSTTY_ENUM_TYPED {
+  GHOSTTY_TERMINAL_PROTECTED_MODE_OFF = 0,
+  GHOSTTY_TERMINAL_PROTECTED_MODE_ISO = 1,
+  GHOSTTY_TERMINAL_PROTECTED_MODE_DEC = 2,
+  GHOSTTY_TERMINAL_PROTECTED_MODE_MAX_VALUE = GHOSTTY_ENUM_MAX_VALUE,
+} GhosttyTerminalProtectedMode;
+
+/** Shell-integration click behavior retained by OSC 133 state. */
+typedef enum GHOSTTY_ENUM_TYPED {
+  GHOSTTY_TERMINAL_SEMANTIC_CLICK_NONE = 0,
+  GHOSTTY_TERMINAL_SEMANTIC_CLICK_EVENTS_ABSOLUTE = 1,
+  GHOSTTY_TERMINAL_SEMANTIC_CLICK_EVENTS_RELATIVE = 2,
+  GHOSTTY_TERMINAL_SEMANTIC_CLICK_LINE = 3,
+  GHOSTTY_TERMINAL_SEMANTIC_CLICK_MULTIPLE = 4,
+  GHOSTTY_TERMINAL_SEMANTIC_CLICK_CONSERVATIVE_VERTICAL = 5,
+  GHOSTTY_TERMINAL_SEMANTIC_CLICK_SMART_VERTICAL = 6,
+  GHOSTTY_TERMINAL_SEMANTIC_CLICK_MAX_VALUE = GHOSTTY_ENUM_MAX_VALUE,
+} GhosttyTerminalSemanticClick;
+
+/** Fixed capacity of Ghostty's per-screen Kitty keyboard flag stack. */
+#define GHOSTTY_TERMINAL_KITTY_KEYBOARD_STACK_CAPACITY 8
+
+/** Read-only state and dimensions for one terminal screen. */
+typedef struct {
+  /** Size of this struct in bytes. Must be initialized with GHOSTTY_INIT_SIZED. */
+  size_t size;
+  uint16_t cols;
+  uint16_t rows;
+  uint64_t total_rows;
+  uint64_t scrollback_rows;
+  uint16_t cursor_x;
+  uint16_t cursor_y;
+  GhosttyTerminalCursorStyle cursor_visual_style;
+  GhosttyStyle cursor_style;
+  bool cursor_protected;
+  bool cursor_pending_wrap;
+  GhosttyTerminalCharsetState charset;
+  /** Current Kitty keyboard flags, retained for source compatibility. */
+  uint8_t kitty_keyboard_flags;
+  /** Viewport row offset from the oldest retained screen row. */
+  uint64_t viewport_offset;
+  GhosttyCellSemanticContent cursor_semantic_content;
+  bool cursor_semantic_content_clear_eol;
+  /** Counter used to mint the next implicit OSC 8 identity. */
+  uint64_t hyperlink_implicit_id;
+  GhosttyTerminalProtectedMode protected_mode;
+  uint8_t kitty_keyboard_stack[GHOSTTY_TERMINAL_KITTY_KEYBOARD_STACK_CAPACITY];
+  uint8_t kitty_keyboard_index;
+  bool semantic_prompt_seen;
+  GhosttyTerminalSemanticClick semantic_prompt_click;
+} GhosttyTerminalScreenState;
+
@@ -1425,0 +1522,86 @@ GHOSTTY_API GhosttyResult ghostty_terminal_mode_set(GhosttyTerminal terminal,
+/**
+ * Read the DECSC saved-cursor slot belonging to a specific screen.
+ *
+ * This does not switch screens or mutate terminal state. Alternate screens are
+ * initialized lazily; an uninitialized screen or an empty slot returns
+ * GHOSTTY_NO_VALUE.
+ *
+ * @param terminal The terminal handle
+ * @param screen Screen whose saved slot should be read
+ * @param[out] out_cursor Receives the complete saved cursor state
+ * @return GHOSTTY_SUCCESS, GHOSTTY_NO_VALUE, or GHOSTTY_INVALID_VALUE
+ */
+GHOSTTY_API GhosttyResult ghostty_terminal_saved_cursor(
+    GhosttyTerminal terminal,
+    GhosttyTerminalScreen screen,
+    GhosttyTerminalSavedCursor* out_cursor);
+
+/**
+ * Read current state and dimensions from a specific screen without activating
+ * it. An alternate screen that has not been initialized returns
+ * GHOSTTY_NO_VALUE.
+ */
+GHOSTTY_API GhosttyResult ghostty_terminal_screen_get(
+    GhosttyTerminal terminal,
+    GhosttyTerminalScreen screen,
+    GhosttyTerminalScreenState* out_state);
+
+/**
+ * Resolve a point against a specific screen without activating it.
+ *
+ * Point tags have the same meanings and costs as ghostty_terminal_grid_ref().
+ * The returned reference has the usual untracked-reference lifetime.
+ */
+GHOSTTY_API GhosttyResult ghostty_terminal_screen_grid_ref(
+    GhosttyTerminal terminal,
+    GhosttyTerminalScreen screen,
+    GhosttyPoint point,
+    GhosttyGridRef* out_ref);
+
+/**
+ * Create a sequential iterator over every retained row of a selected screen.
+ *
+ * Unlike repeated screen-coordinate lookups, iteration traverses the backing
+ * page list once. The terminal must remain alive and unmodified until the
+ * iterator is freed.
+ */
+GHOSTTY_API GhosttyResult ghostty_terminal_screen_row_iterator_new(
+    const GhosttyAllocator* allocator,
+    GhosttyTerminal terminal,
+    GhosttyTerminalScreen screen,
+    GhosttyTerminalScreenRowIterator* out_iterator);
+
+/** Free a screen row iterator. NULL is allowed. */
+GHOSTTY_API void ghostty_terminal_screen_row_iterator_free(
+    GhosttyTerminalScreenRowIterator iterator);
+
+/**
+ * Advance a screen row iterator.
+ *
+ * On success, `out_has_value` says whether a row was produced. A produced
+ * reference points at column zero of that row; callers may copy it and change
+ * only `x` to inspect other cells in the same row.
+ */
+GHOSTTY_API GhosttyResult ghostty_terminal_screen_row_iterator_next(
+    GhosttyTerminalScreenRowIterator iterator,
+    GhosttyGridRef* out_ref,
+    bool* out_has_value);
+
+/**
+ * Read the currently open OSC 8 hyperlink on a selected screen's cursor.
+ *
+ * This is separate from GhosttyTerminalScreenState because URI and explicit
+ * identity are variable-length. A cursor without an open hyperlink returns
+ * GHOSTTY_NO_VALUE. Buffer sizing follows the grid-ref hyperlink accessors.
+ */
+GHOSTTY_API GhosttyResult ghostty_terminal_screen_cursor_hyperlink(
+    GhosttyTerminal terminal,
+    GhosttyTerminalScreen screen,
+    uint8_t* uri_buf,
+    size_t uri_buf_len,
+    size_t* out_uri_len,
+    uint8_t* id_buf,
+    size_t id_buf_len,
+    size_t* out_id_len,
+    GhosttyHyperlinkIdentity* out_identity);
+
diff --git a/include/ghostty/vt/types.h b/include/ghostty/vt/types.h
index 214d282296c5f66aa29b4c9b183655f9c6793ac5..5d0def15cf39a65f72dcbc0174c58399a52c275c 100644
--- a/include/ghostty/vt/types.h
+++ b/include/ghostty/vt/types.h
@@ -159,0 +160,10 @@ typedef struct GhosttyRenderStateRowCellsImpl* GhosttyRenderStateRowCells;
+/**
+ * Opaque iterator over every retained row of one selected terminal screen.
+ *
+ * The iterator borrows the terminal. The terminal must not be mutated or
+ * freed until the iterator is freed.
+ *
+ * @ingroup terminal
+ */
+typedef struct GhosttyTerminalScreenRowIteratorImpl* GhosttyTerminalScreenRowIterator;
+
diff --git a/src/lib_vt.zig b/src/lib_vt.zig
index e01cdbb892c36cf88d4088c45bb464a429654d57..b1b2b10086b74980b9b1074b95a52d5ae241da17 100644
--- a/src/lib_vt.zig
+++ b/src/lib_vt.zig
@@ -230,0 +231 @@ comptime {
+        @export(&c.formatter_terminal_state_new, .{ .name = "ghostty_formatter_terminal_state_new" });
@@ -269,0 +271,7 @@ comptime {
+        @export(&c.terminal_saved_cursor, .{ .name = "ghostty_terminal_saved_cursor" });
+        @export(&c.terminal_screen_get, .{ .name = "ghostty_terminal_screen_get" });
+        @export(&c.terminal_screen_grid_ref, .{ .name = "ghostty_terminal_screen_grid_ref" });
+        @export(&c.terminal_screen_row_iterator_new, .{ .name = "ghostty_terminal_screen_row_iterator_new" });
+        @export(&c.terminal_screen_row_iterator_free, .{ .name = "ghostty_terminal_screen_row_iterator_free" });
+        @export(&c.terminal_screen_row_iterator_next, .{ .name = "ghostty_terminal_screen_row_iterator_next" });
+        @export(&c.terminal_screen_cursor_hyperlink, .{ .name = "ghostty_terminal_screen_cursor_hyperlink" });
@@ -311,0 +320 @@ comptime {
+        @export(&c.grid_ref_hyperlink_identity, .{ .name = "ghostty_grid_ref_hyperlink_identity" });
diff --git a/src/terminal/Screen.zig b/src/terminal/Screen.zig
index 6c4db3237c84ace7079e2f2eaa7b379580667d45..e07a98dd3ea48cef129e0cd147091397b55c059c 100644
--- a/src/terminal/Screen.zig
+++ b/src/terminal/Screen.zig
@@ -1315 +1315 @@ pub fn cursorCopy(self: *Screen, other: Cursor, opts: struct {
-            // This shouldn't happen because startHyperlink should handle
+            // This shouldn't happen because startHyperlink handles
@@ -1394,6 +1394,5 @@ inline fn cursorChangePin(self: *Screen, new: Pin) void {
-        // Re-add
-        self.startHyperlink(link.uri, switch (link.id) {
-            .explicit => |v| v,
-            .implicit => null,
-        }) catch |err| {
-            // This shouldn't happen because startHyperlink should handle
+        // Re-add the same semantic link. Reopening an implicit link through
+        // startHyperlink would mint a new screen counter value and split one
+        // live OSC 8 run merely because it crossed a page boundary.
+        self.startHyperlinkValue(link.*) catch |err| {
+            // This shouldn't happen because startHyperlinkValue handles
@@ -2440 +2439,10 @@ pub fn startHyperlink(
-    // Loop until we have enough page memory to add the hyperlink
+    return self.startHyperlinkValue(link);
+}
+
+/// Add a fully decoded semantic hyperlink, preserving both explicit bytes and
+/// an already-minted implicit identity across internal page migrations.
+fn startHyperlinkValue(
+    self: *Screen,
+    link: hyperlink.Hyperlink,
+) PageList.IncreaseCapacityError!void {
+    // Loop until we have enough page memory to add the hyperlink.
diff --git a/src/terminal/c/formatter.zig b/src/terminal/c/formatter.zig
index 5b4504c8c8f2dd7767e2b3d55aaf42bcc7055617..6139976f1a41f9445cf4f4f41dad9081c225df7f 100644
--- a/src/terminal/c/formatter.zig
+++ b/src/terminal/c/formatter.zig
@@ -115,0 +116,26 @@ pub fn terminal_new(
+        false,
+    ) catch |err| {
+        result.* = null;
+        return switch (err) {
+            error.InvalidValue => .invalid_value,
+            error.OutOfMemory => .out_of_memory,
+        };
+    };
+
+    return .success;
+}
+
+/// Create a terminal formatter whose content is `.none`. Keeping this as an
+/// additive entry point avoids changing the calling convention of the
+/// by-value C options struct.
+pub fn terminal_state_new(
+    alloc_: ?*const CAllocator,
+    result: *Formatter,
+    terminal_: terminal_c.Terminal,
+    opts: TerminalOptions,
+) callconv(lib.calling_conv) Result {
+    result.* = terminal_new_(
+        alloc_,
+        terminal_,
+        opts,
+        true,
@@ -130,0 +157 @@ fn terminal_new_(
+    state_only: bool,
@@ -150,4 +177,8 @@ fn terminal_new_(
-    if (opts.selection) |sel| formatter.content = .{
-        .selection = sel.toZig() orelse
-            return error.InvalidValue,
-    };
+    if (state_only) {
+        formatter.content = .none;
+    } else if (opts.selection) |sel| {
+        formatter.content = .{
+            .selection = sel.toZig() orelse
+                return error.InvalidValue,
+        };
+    }
@@ -405,0 +437,63 @@ test "format vt" {
+test "terminal_state_new emits state without screen content" {
+    var source: terminal_c.Terminal = null;
+    try testing.expectEqual(Result.success, terminal_c.new(
+        &lib.alloc.test_allocator,
+        &source,
+        .{ .cols = 12, .rows = 3, .max_scrollback = 10_000 },
+    ));
+    defer terminal_c.free(source);
+
+    terminal_c.vt_write(source, "secret\x1b[?2004h", 14);
+
+    var formatter: Formatter = null;
+    try testing.expectEqual(Result.success, terminal_state_new(
+        &lib.alloc.test_allocator,
+        &formatter,
+        source,
+        .{
+            .emit = .vt,
+            .unwrap = false,
+            .trim = false,
+            .extra = .{
+                .palette = false,
+                .modes = true,
+                .scrolling_region = false,
+                .tabstops = false,
+                .pwd = false,
+                .keyboard = false,
+                .screen = .{
+                    .cursor = false,
+                    .style = false,
+                    .hyperlink = false,
+                    .protection = false,
+                    .kitty_keyboard = false,
+                    .charsets = false,
+                },
+            },
+        },
+    ));
+    defer free(formatter);
+
+    var buf: [4096]u8 = undefined;
+    var written: usize = 0;
+    try testing.expectEqual(
+        Result.success,
+        format_buf(formatter, &buf, buf.len, &written),
+    );
+    try testing.expect(std.mem.indexOf(u8, buf[0..written], "secret") == null);
+
+    var replacement: terminal_c.Terminal = null;
+    try testing.expectEqual(Result.success, terminal_c.new(
+        &lib.alloc.test_allocator,
+        &replacement,
+        .{ .cols = 12, .rows = 3, .max_scrollback = 10_000 },
+    ));
+    defer terminal_c.free(replacement);
+    terminal_c.vt_write(replacement, buf[0..written].ptr, written);
+
+    try testing.expect(replacement.?.terminal.modes.get(.bracketed_paste));
+    const text = try replacement.?.terminal.plainString(testing.allocator);
+    defer testing.allocator.free(text);
+    try testing.expectEqualStrings("", text);
+}
+
diff --git a/src/terminal/c/grid_ref.zig b/src/terminal/c/grid_ref.zig
index 946ca8a1e36c35cbc305e6f18fc6325e7b744c43..a407153b73b0d93ab1773ca2a29df06b4091bde0 100644
--- a/src/terminal/c/grid_ref.zig
+++ b/src/terminal/c/grid_ref.zig
@@ -42,0 +43,14 @@ pub const CGridRef = extern struct {
+/// C: GhosttyHyperlinkIdentityTag
+pub const HyperlinkIdentityTag = enum(c_int) {
+    none = 0,
+    explicit = 1,
+    implicit = 2,
+};
+
+/// C: GhosttyHyperlinkIdentity
+pub const HyperlinkIdentity = extern struct {
+    size: usize = @sizeOf(HyperlinkIdentity),
+    tag: HyperlinkIdentityTag = .none,
+    implicit_token: u64 = 0,
+};
+
@@ -129,0 +144,37 @@ pub fn grid_ref_hyperlink_uri(
+pub fn grid_ref_hyperlink_identity(
+    ref: *const CGridRef,
+    out_buf: ?[*]u8,
+    buf_len: usize,
+    out_len: *usize,
+    out_identity: ?*HyperlinkIdentity,
+) callconv(lib.calling_conv) Result {
+    const identity = out_identity orelse return .invalid_value;
+    if (identity.size < @sizeOf(HyperlinkIdentity)) return .invalid_value;
+    const p = ref.toPin() orelse return .invalid_value;
+    identity.tag = .none;
+    identity.implicit_token = 0;
+    out_len.* = 0;
+
+    const terminal_page = p.node.page();
+    const cell = terminal_page.getRowAndCell(p.x, p.y).cell;
+    if (!cell.hyperlink) return .success;
+
+    const link_id = terminal_page.lookupHyperlink(cell) orelse return .success;
+    const entry = terminal_page.hyperlink_set.get(terminal_page.memory, link_id);
+    switch (entry.id) {
+        .implicit => |token| {
+            identity.tag = .implicit;
+            identity.implicit_token = @intCast(token);
+        },
+        .explicit => |slice| {
+            identity.tag = .explicit;
+            const id = slice.slice(terminal_page.memory);
+            out_len.* = id.len;
+            if (id.len == 0) return .success;
+            if (out_buf == null or buf_len < id.len) return .out_of_space;
+            @memcpy(out_buf.?[0..id.len], id);
+        },
+    }
+    return .success;
+}
+
@@ -255,0 +307,118 @@ test "grid_ref_hyperlink_uri with hyperlink" {
+
+test "grid_ref_hyperlink_identity preserves explicit bytes" {
+    var terminal: terminal_c.Terminal = null;
+    try testing.expectEqual(Result.success, terminal_c.new(
+        &lib.alloc.test_allocator,
+        &terminal,
+        .{ .cols = 80, .rows = 24, .max_scrollback = 10_000 },
+    ));
+    defer terminal_c.free(terminal);
+
+    const seq =
+        "\x1b]8;id=original-id;https://example.com\x1b\\link\x1b]8;;\x1b\\";
+    terminal_c.vt_write(terminal, seq, seq.len);
+
+    var ref: CGridRef = .{};
+    try testing.expectEqual(Result.success, terminal_c.grid_ref(
+        terminal,
+        point.Point.cval(.{ .active = .{ .x = 0, .y = 0 } }),
+        &ref,
+    ));
+
+    var identity: HyperlinkIdentity = .{};
+    var len: usize = 0;
+    try testing.expectEqual(
+        Result.out_of_space,
+        grid_ref_hyperlink_identity(&ref, null, 0, &len, &identity),
+    );
+    try testing.expectEqual(HyperlinkIdentityTag.explicit, identity.tag);
+    try testing.expectEqual(@as(usize, "original-id".len), len);
+
+    var buf: [32]u8 = undefined;
+    try testing.expectEqual(
+        Result.success,
+        grid_ref_hyperlink_identity(&ref, &buf, buf.len, &len, &identity),
+    );
+    try testing.expectEqualStrings("original-id", buf[0..len]);
+
+    identity.size = 0;
+    try testing.expectEqual(
+        Result.invalid_value,
+        grid_ref_hyperlink_identity(&ref, null, 0, &len, &identity),
+    );
+}
+
+test "grid_ref implicit hyperlink identity is stable across pages" {
+    var terminal: terminal_c.Terminal = null;
+    try testing.expectEqual(Result.success, terminal_c.new(
+        &lib.alloc.test_allocator,
+        &terminal,
+        .{ .cols = 215, .rows = 2, .max_scrollback = 20_000_000 },
+    ));
+    defer terminal_c.free(terminal);
+
+    const open = "\x1b]8;;https://example.com/implicit\x1b\\";
+    terminal_c.vt_write(terminal, open, open.len);
+    for (0..300) |_| terminal_c.vt_write(terminal, "x\r\n", 3);
+    const close = "x\x1b]8;;\x1b\\";
+    terminal_c.vt_write(terminal, close, close.len);
+
+    var state: terminal_c.ScreenState = undefined;
+    state.size = @sizeOf(terminal_c.ScreenState);
+    try testing.expectEqual(
+        Result.success,
+        terminal_c.screen_get(terminal, .primary, &state),
+    );
+    try testing.expect(state.total_rows > 215);
+
+    var first: CGridRef = .{};
+    try testing.expectEqual(Result.success, terminal_c.screen_grid_ref(
+        terminal,
+        .primary,
+        point.Point.cval(.{ .screen = .{ .x = 0, .y = 0 } }),
+        &first,
+    ));
+    var last: CGridRef = .{};
+    try testing.expectEqual(Result.success, terminal_c.screen_grid_ref(
+        terminal,
+        .primary,
+        point.Point.cval(.{
+            .screen = .{
+                .x = 0,
+                .y = @intCast(state.total_rows - 1),
+            },
+        }),
+        &last,
+    ));
+    try testing.expect(first.node != last.node);
+
+    var first_identity: HyperlinkIdentity = .{};
+    var last_identity: HyperlinkIdentity = .{};
+    var len: usize = 0;
+    try testing.expectEqual(
+        Result.success,
+        grid_ref_hyperlink_identity(
+            &first,
+            null,
+            0,
+            &len,
+            &first_identity,
+        ),
+    );
+    try testing.expectEqual(
+        Result.success,
+        grid_ref_hyperlink_identity(
+            &last,
+            null,
+            0,
+            &len,
+            &last_identity,
+        ),
+    );
+    try testing.expectEqual(HyperlinkIdentityTag.implicit, first_identity.tag);
+    try testing.expectEqual(HyperlinkIdentityTag.implicit, last_identity.tag);
+    try testing.expectEqual(
+        first_identity.implicit_token,
+        last_identity.implicit_token,
+    );
+}
diff --git a/src/terminal/c/main.zig b/src/terminal/c/main.zig
index 37dc57684aa5e6edea164acb3d4faa37955667b3..bd49b01f84ef1a67cf7dd6489563b616aac90299 100644
--- a/src/terminal/c/main.zig
+++ b/src/terminal/c/main.zig
@@ -78,0 +79 @@ pub const formatter_terminal_new = formatter.terminal_new;
+pub const formatter_terminal_state_new = formatter.terminal_state_new;
@@ -191,0 +193,7 @@ pub const terminal_get_multi = terminal.get_multi;
+pub const terminal_saved_cursor = terminal.saved_cursor;
+pub const terminal_screen_get = terminal.screen_get;
+pub const terminal_screen_grid_ref = terminal.screen_grid_ref;
+pub const terminal_screen_row_iterator_new = terminal.screen_row_iterator_new;
+pub const terminal_screen_row_iterator_free = terminal.screen_row_iterator_free;
+pub const terminal_screen_row_iterator_next = terminal.screen_row_iterator_next;
+pub const terminal_screen_cursor_hyperlink = terminal.screen_cursor_hyperlink;
@@ -225,0 +234 @@ pub const grid_ref_hyperlink_uri = grid_ref.grid_ref_hyperlink_uri;
+pub const grid_ref_hyperlink_identity = grid_ref.grid_ref_hyperlink_identity;
diff --git a/src/terminal/c/terminal.zig b/src/terminal/c/terminal.zig
index 1c3b9f0c557aa603cf40eab920623eba07cdc7e8..7eafd01b2271c57342d8a3ea649ccbb0c4126606 100644
--- a/src/terminal/c/terminal.zig
+++ b/src/terminal/c/terminal.zig
@@ -14,0 +15 @@ const modes = @import("../modes.zig");
+const charsets = @import("../charsets.zig");
@@ -624,0 +626,9 @@ pub const TerminalCursorStyle = enum(c_int) {
+
+    fn fromZig(value: Screen.CursorStyle) TerminalCursorStyle {
+        return switch (value) {
+            .bar => .bar,
+            .block => .block,
+            .underline => .underline,
+            .block_hollow => .block_hollow,
+        };
+    }
@@ -720,0 +731,131 @@ pub const TerminalScreen = ScreenSet.Key;
+const ScreenRowIteratorWrapper = struct {
+    alloc: std.mem.Allocator,
+    rows: PageList.RowIterator,
+};
+
+/// C: GhosttyTerminalScreenRowIterator
+pub const ScreenRowIterator = ?*ScreenRowIteratorWrapper;
+
+/// C: GhosttyTerminalCharset
+pub const TerminalCharset = charsets.Charset;
+
+/// C: GhosttyTerminalCharsetSlot
+pub const TerminalCharsetSlot = charsets.Slots;
+
+/// C: GhosttyTerminalProtectedMode
+pub const TerminalProtectedMode = enum(c_int) {
+    off = 0,
+    iso = 1,
+    dec = 2,
+
+    fn fromZig(value: @import("../ansi.zig").ProtectedMode) TerminalProtectedMode {
+        return switch (value) {
+            .off => .off,
+            .iso => .iso,
+            .dec => .dec,
+        };
+    }
+};
+
+/// C: GhosttyTerminalSemanticClick
+pub const TerminalSemanticClick = enum(c_int) {
+    none = 0,
+    click_events_absolute = 1,
+    click_events_relative = 2,
+    line = 3,
+    multiple = 4,
+    conservative_vertical = 5,
+    smart_vertical = 6,
+
+    fn fromZig(value: Screen.SemanticPrompt.SemanticClick) TerminalSemanticClick {
+        return switch (value) {
+            .none => .none,
+            .click_events => |click| switch (click) {
+                .absolute => .click_events_absolute,
+                .relative => .click_events_relative,
+            },
+            .cl => |click| switch (click) {
+                .line => .line,
+                .multiple => .multiple,
+                .conservative_vertical => .conservative_vertical,
+                .smart_vertical => .smart_vertical,
+            },
+        };
+    }
+};
+
+/// C: GhosttyTerminalCharsetState
+pub const TerminalCharsetState = extern struct {
+    g0: TerminalCharset,
+    g1: TerminalCharset,
+    g2: TerminalCharset,
+    g3: TerminalCharset,
+    gl: TerminalCharsetSlot,
+    gr: TerminalCharsetSlot,
+    has_single_shift: bool,
+    single_shift: TerminalCharsetSlot,
+
+    fn fromZig(value: Screen.CharsetState) TerminalCharsetState {
+        return .{
+            .g0 = value.charsets.g0,
+            .g1 = value.charsets.g1,
+            .g2 = value.charsets.g2,
+            .g3 = value.charsets.g3,
+            .gl = value.gl,
+            .gr = value.gr,
+            .has_single_shift = value.single_shift != null,
+            .single_shift = value.single_shift orelse .G0,
+        };
+    }
+};
+
+/// C: GhosttyTerminalSavedCursor
+pub const SavedCursor = extern struct {
+    size: usize = @sizeOf(SavedCursor),
+    x: size.CellCountInt,
+    y: size.CellCountInt,
+    style: style_c.Style,
+    protected_cell: bool,
+    pending_wrap: bool,
+    origin: bool,
+    charset: TerminalCharsetState,
+
+    fn fromZig(value: Screen.SavedCursor) SavedCursor {
+        return .{
+            .x = value.x,
+            .y = value.y,
+            .style = .fromStyle(value.style),
+            .protected_cell = value.protected,
+            .pending_wrap = value.pending_wrap,
+            .origin = value.origin,
+            .charset = .fromZig(value.charset),
+        };
+    }
+};
+
+/// C: GhosttyTerminalScreenState
+pub const ScreenState = extern struct {
+    size: usize = @sizeOf(ScreenState),
+    cols: size.CellCountInt,
+    rows: size.CellCountInt,
+    total_rows: u64,
+    scrollback_rows: u64,
+    cursor_x: size.CellCountInt,
+    cursor_y: size.CellCountInt,
+    cursor_visual_style: TerminalCursorStyle,
+    cursor_style: style_c.Style,
+    cursor_protected: bool,
+    cursor_pending_wrap: bool,
+    charset: TerminalCharsetState,
+    kitty_keyboard_flags: u8,
+    viewport_offset: u64,
+    cursor_semantic_content: cell_c.SemanticContent,
+    cursor_semantic_content_clear_eol: bool,
+    hyperlink_implicit_id: u64,
+    protected_mode: TerminalProtectedMode,
+    kitty_keyboard_stack: [8]u8,
+    kitty_keyboard_index: u8,
+    semantic_prompt_seen: bool,
+    semantic_prompt_click: TerminalSemanticClick,
+};
+
@@ -834,0 +976,188 @@ pub fn get_multi(
+pub fn saved_cursor(
+    terminal_: Terminal,
+    screen_key: TerminalScreen,
+    out: ?*SavedCursor,
+) callconv(lib.calling_conv) Result {
+    const t: *ZigTerminal = (terminal_ orelse return .invalid_value).terminal;
+    const result = out orelse return .invalid_value;
+    if (result.size < @sizeOf(SavedCursor)) return .invalid_value;
+    if (comptime std.debug.runtime_safety) {
+        _ = std.meta.intToEnum(TerminalScreen, @intFromEnum(screen_key)) catch
+            return .invalid_value;
+    }
+    const screen = t.screens.get(screen_key) orelse return .no_value;
+    const value = screen.saved_cursor orelse return .no_value;
+    result.* = .fromZig(value);
+    return .success;
+}
+
+pub fn screen_get(
+    terminal_: Terminal,
+    screen_key: TerminalScreen,
+    out: ?*ScreenState,
+) callconv(lib.calling_conv) Result {
+    const t: *ZigTerminal = (terminal_ orelse return .invalid_value).terminal;
+    const result = out orelse return .invalid_value;
+    if (result.size < @sizeOf(ScreenState)) return .invalid_value;
+    if (comptime std.debug.runtime_safety) {
+        _ = std.meta.intToEnum(TerminalScreen, @intFromEnum(screen_key)) catch
+            return .invalid_value;
+    }
+    const screen = t.screens.get(screen_key) orelse return .no_value;
+    const total_rows: u64 = @intCast(screen.pages.total_rows);
+    const scrollbar = screen.pages.scrollbar();
+    var kitty_keyboard_stack: [8]u8 = undefined;
+    for (screen.kitty_keyboard.flags, 0..) |flags, index| {
+        kitty_keyboard_stack[index] = @as(u8, flags.int());
+    }
+    result.* = .{
+        .cols = t.cols,
+        .rows = t.rows,
+        .total_rows = total_rows,
+        .scrollback_rows = total_rows - @as(u64, t.rows),
+        .cursor_x = screen.cursor.x,
+        .cursor_y = screen.cursor.y,
+        .cursor_visual_style = .fromZig(screen.cursor.cursor_style),
+        .cursor_style = .fromStyle(screen.cursor.style),
+        .cursor_protected = screen.cursor.protected,
+        .cursor_pending_wrap = screen.cursor.pending_wrap,
+        .charset = .fromZig(screen.charset),
+        .kitty_keyboard_flags = @as(u8, screen.kitty_keyboard.current().int()),
+        .viewport_offset = @intCast(scrollbar.offset),
+        .cursor_semantic_content = @enumFromInt(
+            @intFromEnum(screen.cursor.semantic_content),
+        ),
+        .cursor_semantic_content_clear_eol = screen.cursor.semantic_content_clear_eol,
+        .hyperlink_implicit_id = @intCast(screen.cursor.hyperlink_implicit_id),
+        .protected_mode = .fromZig(screen.protected_mode),
+        .kitty_keyboard_stack = kitty_keyboard_stack,
+        .kitty_keyboard_index = @intCast(screen.kitty_keyboard.idx),
+        .semantic_prompt_seen = screen.semantic_prompt.seen,
+        .semantic_prompt_click = .fromZig(screen.semantic_prompt.click),
+    };
+    return .success;
+}
+
+pub fn screen_grid_ref(
+    terminal_: Terminal,
+    screen_key: TerminalScreen,
+    pt: point.Point.C,
+    out_ref: ?*grid_ref_c.CGridRef,
+) callconv(lib.calling_conv) Result {
+    const t: *ZigTerminal = (terminal_ orelse return .invalid_value).terminal;
+    if (comptime std.debug.runtime_safety) {
+        _ = std.meta.intToEnum(TerminalScreen, @intFromEnum(screen_key)) catch
+            return .invalid_value;
+    }
+    const screen = t.screens.get(screen_key) orelse return .no_value;
+    const p = screen.pages.pin(.fromC(pt)) orelse return .invalid_value;
+    if (out_ref) |out| out.* = grid_ref_c.CGridRef.fromPin(p);
+    return .success;
+}
+
+pub fn screen_row_iterator_new(
+    alloc_: ?*const CAllocator,
+    terminal_: Terminal,
+    screen_key: TerminalScreen,
+    out: ?*ScreenRowIterator,
+) callconv(lib.calling_conv) Result {
+    const result = out orelse return .invalid_value;
+    // Constructor APIs leave a caller-safe value on every failure path.
+    result.* = null;
+    const t: *ZigTerminal = (terminal_ orelse return .invalid_value).terminal;
+    if (comptime std.debug.runtime_safety) {
+        _ = std.meta.intToEnum(TerminalScreen, @intFromEnum(screen_key)) catch
+            return .invalid_value;
+    }
+    const screen = t.screens.get(screen_key) orelse return .no_value;
+    const alloc = lib.alloc.default(alloc_);
+    const ptr = alloc.create(ScreenRowIteratorWrapper) catch
+        return .out_of_memory;
+    ptr.* = .{
+        .alloc = alloc,
+        .rows = screen.pages.rowIterator(
+            .right_down,
+            .{ .screen = .{} },
+            null,
+        ),
+    };
+    result.* = ptr;
+    return .success;
+}
+
+pub fn screen_row_iterator_free(
+    iterator_: ScreenRowIterator,
+) callconv(lib.calling_conv) void {
+    const iterator = iterator_ orelse return;
+    const alloc = iterator.alloc;
+    alloc.destroy(iterator);
+}
+
+pub fn screen_row_iterator_next(
+    iterator_: ScreenRowIterator,
+    out_ref: ?*grid_ref_c.CGridRef,
+    out_has_value: ?*bool,
+) callconv(lib.calling_conv) Result {
+    const iterator = iterator_ orelse return .invalid_value;
+    const result = out_ref orelse return .invalid_value;
+    const has_value = out_has_value orelse return .invalid_value;
+    const pin = iterator.rows.next() orelse {
+        has_value.* = false;
+        result.* = .{};
+        return .success;
+    };
+    result.* = .fromPin(pin);
+    has_value.* = true;
+    return .success;
+}
+
+pub fn screen_cursor_hyperlink(
+    terminal_: Terminal,
+    screen_key: TerminalScreen,
+    uri_buf: ?[*]u8,
+    uri_buf_len: usize,
+    out_uri_len: ?*usize,
+    id_buf: ?[*]u8,
+    id_buf_len: usize,
+    out_id_len: ?*usize,
+    out_identity: ?*grid_ref_c.HyperlinkIdentity,
+) callconv(lib.calling_conv) Result {
+    const t: *ZigTerminal = (terminal_ orelse return .invalid_value).terminal;
+    const uri_len = out_uri_len orelse return .invalid_value;
+    const id_len = out_id_len orelse return .invalid_value;
+    const identity = out_identity orelse return .invalid_value;
+    if (identity.size < @sizeOf(grid_ref_c.HyperlinkIdentity))
+        return .invalid_value;
+    if (comptime std.debug.runtime_safety) {
+        _ = std.meta.intToEnum(TerminalScreen, @intFromEnum(screen_key)) catch
+            return .invalid_value;
+    }
+    const screen = t.screens.get(screen_key) orelse return .no_value;
+    const link = screen.cursor.hyperlink orelse return .no_value;
+
+    uri_len.* = link.uri.len;
+    id_len.* = 0;
+    identity.tag = .none;
+    identity.implicit_token = 0;
+    switch (link.id) {
+        .explicit => |id| {
+            identity.tag = .explicit;
+            id_len.* = id.len;
+        },
+        .implicit => |token| {
+            identity.tag = .implicit;
+            identity.implicit_token = @intCast(token);
+        },
+    }
+
+    if (uri_buf_len < uri_len.* or (uri_len.* != 0 and uri_buf == null) or
+        id_buf_len < id_len.* or (id_len.* != 0 and id_buf == null))
+        return .out_of_space;
+    if (uri_len.* != 0) @memcpy(uri_buf.?[0..uri_len.*], link.uri);
+    if (id_len.* != 0) switch (link.id) {
+        .explicit => |id| @memcpy(id_buf.?[0..id_len.*], id),
+        .implicit => unreachable,
+    };
+    return .success;
+}
+
@@ -1472,0 +1802,243 @@ test "mode_set unknown mode" {
+test "saved cursor and screen reads are screen selected" {
+    var t: Terminal = null;
+    try testing.expectEqual(Result.success, new(
+        &lib.alloc.test_allocator,
+        &t,
+        .{
+            .cols = 8,
+            .rows = 3,
+            .max_scrollback = 10_000,
+        },
+    ));
+    defer free(t);
+
+    var saved: SavedCursor = undefined;
+    saved.size = @sizeOf(SavedCursor);
+    try testing.expectEqual(
+        Result.no_value,
+        saved_cursor(t, .primary, &saved),
+    );
+    var missing_iterator: ScreenRowIterator = null;
+    try testing.expectEqual(
+        Result.no_value,
+        screen_row_iterator_new(
+            &lib.alloc.test_allocator,
+            t,
+            .alternate,
+            &missing_iterator,
+        ),
+    );
+    try testing.expectEqual(@as(ScreenRowIterator, null), missing_iterator);
+    saved.size = 0;
+    try testing.expectEqual(
+        Result.invalid_value,
+        saved_cursor(t, .primary, &saved),
+    );
+    saved.size = @sizeOf(SavedCursor);
+
+    const primary_content = "primary\r\nhistory\r\n";
+    vt_write(t, primary_content, primary_content.len);
+    const primary_state =
+        "\x1b[?6h\x1b[1;31m\x1b[1\"q\x1b(0\x1b[2;8HX\x1b7";
+    vt_write(t, primary_state, primary_state.len);
+
+    try testing.expectEqual(
+        Result.success,
+        saved_cursor(t, .primary, &saved),
+    );
+    try testing.expectEqual(@as(size.CellCountInt, 7), saved.x);
+    try testing.expect(saved.style.bold);
+    try testing.expect(saved.protected_cell);
+    try testing.expect(saved.pending_wrap);
+    try testing.expect(saved.origin);
+    try testing.expectEqual(TerminalCharset.dec_special, saved.charset.g0);
+
+    const alternate_state =
+        "\x1b[?1049h\x1b[?6l\x1b[0m\x1b[1\"q\x1b(Balternate\x1b[3;8HX\x1b7";
+    vt_write(t, alternate_state, alternate_state.len);
+
+    var alternate_saved: SavedCursor = undefined;
+    alternate_saved.size = @sizeOf(SavedCursor);
+    try testing.expectEqual(
+        Result.success,
+        saved_cursor(t, .alternate, &alternate_saved),
+    );
+    try testing.expectEqual(@as(size.CellCountInt, 7), alternate_saved.x);
+    try testing.expectEqual(@as(size.CellCountInt, 2), alternate_saved.y);
+    try testing.expect(!alternate_saved.style.bold);
+    try testing.expect(alternate_saved.protected_cell);
+    try testing.expect(alternate_saved.pending_wrap);
+    try testing.expect(!alternate_saved.origin);
+    try testing.expectEqual(TerminalCharset.ascii, alternate_saved.charset.g0);
+
+    // The primary screen stays readable while the alternate screen is active.
+    var primary: ScreenState = undefined;
+    primary.size = @sizeOf(ScreenState);
+    try testing.expectEqual(
+        Result.success,
+        screen_get(t, .primary, &primary),
+    );
+    try testing.expectEqual(@as(size.CellCountInt, 8), primary.cols);
+    try testing.expectEqual(@as(size.CellCountInt, 3), primary.rows);
+    try testing.expect(primary.total_rows >= primary.rows);
+    try testing.expectEqual(
+        primary.total_rows - primary.rows,
+        primary.scrollback_rows,
+    );
+
+    var iterator: ScreenRowIterator = null;
+    try testing.expectEqual(
+        Result.success,
+        screen_row_iterator_new(
+            &lib.alloc.test_allocator,
+            t,
+            .primary,
+            &iterator,
+        ),
+    );
+    defer screen_row_iterator_free(iterator);
+    var iterated_rows: u64 = 0;
+    while (true) {
+        var row_ref: grid_ref_c.CGridRef = .{};
+        var has_row = false;
+        try testing.expectEqual(
+            Result.success,
+            screen_row_iterator_next(iterator, &row_ref, &has_row),
+        );
+        if (!has_row) break;
+        try testing.expectEqual(@as(size.CellCountInt, 0), row_ref.x);
+        iterated_rows += 1;
+    }
+    try testing.expectEqual(primary.total_rows, iterated_rows);
+    var exhausted_ref: grid_ref_c.CGridRef = .{};
+    var exhausted_has_row = true;
+    try testing.expectEqual(
+        Result.success,
+        screen_row_iterator_next(
+            iterator,
+            &exhausted_ref,
+            &exhausted_has_row,
+        ),
+    );
+    try testing.expect(!exhausted_has_row);
+
+    var ref: grid_ref_c.CGridRef = .{};
+    try testing.expectEqual(Result.success, screen_grid_ref(
+        t,
+        .primary,
+        point.Point.cval(.{ .screen = .{ .x = 0, .y = 0 } }),
+        &ref,
+    ));
+    var graphemes: [4]u32 = undefined;
+    var grapheme_len: usize = 0;
+    try testing.expectEqual(
+        Result.success,
+        grid_ref_c.grid_ref_graphemes(
+            &ref,
+            &graphemes,
+            graphemes.len,
+            &grapheme_len,
+        ),
+    );
+    try testing.expectEqual(@as(usize, 1), grapheme_len);
+    try testing.expectEqual(@as(u32, 'p'), graphemes[0]);
+
+    var alternate: ScreenState = undefined;
+    alternate.size = @sizeOf(ScreenState);
+    try testing.expectEqual(
+        Result.success,
+        screen_get(t, .alternate, &alternate),
+    );
+    try testing.expectEqual(@as(size.CellCountInt, 7), alternate.cursor_x);
+    try testing.expectEqual(TerminalCharset.ascii, alternate.charset.g0);
+
+    alternate.size = 0;
+    try testing.expectEqual(
+        Result.invalid_value,
+        screen_get(t, .alternate, &alternate),
+    );
+}
+
+test "screen cursor hyperlink exposes URI and semantic identity" {
+    var t: Terminal = null;
+    try testing.expectEqual(Result.success, new(
+        &lib.alloc.test_allocator,
+        &t,
+        .{ .cols = 12, .rows = 3, .max_scrollback = 10_000 },
+    ));
+    defer free(t);
+
+    var identity: grid_ref_c.HyperlinkIdentity = .{};
+    var uri_len: usize = 0;
+    var id_len: usize = 0;
+    try testing.expectEqual(
+        Result.no_value,
+        screen_cursor_hyperlink(
+            t,
+            .primary,
+            null,
+            0,
+            &uri_len,
+            null,
+            0,
+            &id_len,
+            &identity,
+        ),
+    );
+
+    const explicit = "\x1b]8;id=group;https://example.test/explicit\x1b\\";
+    vt_write(t, explicit, explicit.len);
+    try testing.expectEqual(
+        Result.out_of_space,
+        screen_cursor_hyperlink(
+            t,
+            .primary,
+            null,
+            0,
+            &uri_len,
+            null,
+            0,
+            &id_len,
+            &identity,
+        ),
+    );
+    var uri: [64]u8 = undefined;
+    var id: [16]u8 = undefined;
+    try testing.expectEqual(
+        Result.success,
+        screen_cursor_hyperlink(
+            t,
+            .primary,
+            &uri,
+            uri.len,
+            &uri_len,
+            &id,
+            id.len,
+            &id_len,
+            &identity,
+        ),
+    );
+    try testing.expectEqualStrings("https://example.test/explicit", uri[0..uri_len]);
+    try testing.expectEqualStrings("group", id[0..id_len]);
+    try testing.expectEqual(grid_ref_c.HyperlinkIdentityTag.explicit, identity.tag);
+
+    const implicit = "\x1b]8;;https://example.test/implicit\x1b\\";
+    vt_write(t, implicit, implicit.len);
+    try testing.expectEqual(
+        Result.out_of_space,
+        screen_cursor_hyperlink(
+            t,
+            .primary,
+            null,
+            0,
+            &uri_len,
+            null,
+            0,
+            &id_len,
+            &identity,
+        ),
+    );
+    try testing.expectEqual(grid_ref_c.HyperlinkIdentityTag.implicit, identity.tag);
+    try testing.expectEqual(@as(usize, 0), id_len);
+}
+
diff --git a/src/terminal/c/types.zig b/src/terminal/c/types.zig
index f160f3852bf12aed376abf10a9b62ac1c336abe1..2434505631c6ffe038f753a2d06ee5ad4f04bf04 100644
--- a/src/terminal/c/types.zig
+++ b/src/terminal/c/types.zig
@@ -51,0 +52 @@ pub const structs: std.StaticStringMap(StructInfo) = structs: {
+        .{ "GhosttyHyperlinkIdentity", StructInfo.init(grid_ref.HyperlinkIdentity) },
@@ -70,0 +72 @@ pub const structs: std.StaticStringMap(StructInfo) = structs: {
+        .{ "GhosttyTerminalCharsetState", StructInfo.init(terminal.TerminalCharsetState) },
@@ -71,0 +74,2 @@ pub const structs: std.StaticStringMap(StructInfo) = structs: {
+        .{ "GhosttyTerminalSavedCursor", StructInfo.init(terminal.SavedCursor) },
+        .{ "GhosttyTerminalScreenState", StructInfo.init(terminal.ScreenState) },
@@ -79 +83 @@ pub const json: [:0]const u8 = json: {
-    @setEvalBranchQuota(100000);
+    @setEvalBranchQuota(200_000);
diff --git a/src/terminal/formatter.zig b/src/terminal/formatter.zig
index 553ea28d0930ec68a387ae041a0ffaa338bca997..91d621b641755a45f76106d0dc24543125660365 100644
--- a/src/terminal/formatter.zig
+++ b/src/terminal/formatter.zig
@@ -298,0 +299,58 @@ pub const TerminalFormatter = struct {
+        // DECCOLM (mode 3) is ignored unless mode 40 is already enabled, and a
+        // successful DECCOLM resize recreates the tabstop table. Restore this
+        // pair before custom tabstops and before the generated mode-order loop
+        // (where numeric mode 3 otherwise precedes mode 40).
+        if (self.opts.emit == .vt and self.extra.modes) {
+            const mode_40 = self.terminal.modes.get(.enable_mode_3);
+            const mode_40_default = self.terminal.modes.default.enable_mode_3;
+            if (mode_40 != mode_40_default) {
+                try writer.print(
+                    "\x1b[?40{s}",
+                    .{if (mode_40) "h" else "l"},
+                );
+            }
+
+            const mode_3 = self.terminal.modes.get(.@"132_column");
+            const mode_3_default = self.terminal.modes.default.@"132_column";
+            if (mode_3 != mode_3_default) {
+                try writer.print(
+                    "\x1b[?3{s}",
+                    .{if (mode_3) "h" else "l"},
+                );
+            }
+        }
+
+        // Tabstop restoration moves the cursor. Emit it after DECCOLM but
+        // before the remaining modes and screen content so origin mode and
+        // horizontal margins cannot shift the requested absolute columns.
+        if (self.opts.emit == .vt and self.extra.tabstops) {
+            // Clear all tabs (CSI 3 g)
+            try writer.print("\x1b[3g", .{});
+
+            // Set each configured tabstop by moving cursor and using HTS.
+            for (0..self.terminal.cols) |col| {
+                if (self.terminal.tabstops.get(col)) {
+                    try writer.print("\x1b[{d}G", .{col + 1});
+                    try writer.print("\x1bH", .{});
+                }
+            }
+            // PageListFormatter begins from the terminal home position.
+            try writer.print("\x1b[H", .{});
+
+            if (self.pin_map) |*m| {
+                var discarding: std.Io.Writer.Discarding = .init(&.{});
+                var extra_formatter: TerminalFormatter = self;
+                extra_formatter.content = .none;
+                extra_formatter.pin_map = null;
+                extra_formatter.extra = .none;
+                extra_formatter.extra.tabstops = true;
+                try extra_formatter.format(&discarding.writer);
+
+                m.map.appendNTimes(
+                    m.alloc,
+                    self.terminal.screens.active.pages.getTopLeft(.screen),
+                    std.math.cast(usize, discarding.count) orelse return error.WriteFailed,
+                ) catch return error.WriteFailed;
+            }
+        }
+
@@ -305,0 +364 @@ pub const TerminalFormatter = struct {
+                if (mode == .enable_mode_3 or mode == .@"132_column") continue;
@@ -339,0 +399,3 @@ pub const TerminalFormatter = struct {
+        // Terminal-level extras below can reset or move the cursor. Delay CUP
+        // until all of them have been emitted.
+        if (self.opts.emit == .vt) screen_formatter.extra.cursor = false;
@@ -363,16 +424,0 @@ pub const TerminalFormatter = struct {
-            // Emit tabstop positions
-            if (self.extra.tabstops) {
-                // Clear all tabs (CSI 3 g)
-                try writer.print("\x1b[3g", .{});
-
-                // Set each configured tabstop by moving cursor and using HTS
-                for (0..self.terminal.cols) |col| {
-                    if (self.terminal.tabstops.get(col)) {
-                        // Move cursor to the column (1-indexed)
-                        try writer.print("\x1b[{d}G", .{col + 1});
-                        // Set tab (HTS)
-                        try writer.print("\x1bH", .{});
-                    }
-                }
-            }
-
@@ -392,0 +439,21 @@ pub const TerminalFormatter = struct {
+            // Restore the cursor last. CUP is relative to the margins while
+            // origin mode is active, so translate Ghostty's absolute cursor
+            // coordinates only when this formatter emitted both pieces of
+            // state that make the target interpret CUP that way.
+            if (self.extra.screen.cursor) {
+                const cursor = &self.terminal.screens.active.cursor;
+                const origin = self.extra.modes and
+                    self.terminal.modes.get(.origin) and
+                    self.extra.scrolling_region;
+                const row = if (origin)
+                    cursor.y -| self.terminal.scrolling_region.top
+                else
+                    cursor.y;
+                const column = if (origin and
+                    self.terminal.modes.get(.enable_left_and_right_margin))
+                    cursor.x -| self.terminal.scrolling_region.left
+                else
+                    cursor.x;
+                try writer.print("\x1b[{d};{d}H", .{ row + 1, column + 1 });
+            }
+
@@ -401 +467,0 @@ pub const TerminalFormatter = struct {
-                extra_formatter.extra.tabstops = self.extra.tabstops;
@@ -403,0 +470 @@ pub const TerminalFormatter = struct {
+                extra_formatter.extra.screen.cursor = self.extra.screen.cursor;
@@ -652,0 +720,12 @@ pub const ScreenFormatter = struct {
+
+            // SS2/SS3 applies to exactly the next printable character. It
+            // therefore has to be emitted after all other formatter output
+            // that could contain printable screen content.
+            if (charset.single_shift) |slot| {
+                const seq = switch (slot) {
+                    .G2 => "\x1bN", // SS2
+                    .G3 => "\x1bO", // SS3
+                    .G0, .G1 => unreachable,
+                };
+                try writer.print("{s}", .{seq});
+            }
@@ -4876 +4955 @@ test "Screen vt with kitty keyboard" {
-test "Screen vt with charsets" {
+test "Screen vt with charsets including pending single shift" {
@@ -4892,2 +4971,3 @@ test "Screen vt with charsets" {
-    // Set G0 to DEC special and shift to G1
-    s.nextSlice("\x1b(0\x0ehello");
+    // Set G0 and G2 to DEC special, shift GL to G1, and leave SS2
+    // pending. The formatter must not consume that one-shot state itself.
+    s.nextSlice("\x1b(0\x0ehello\x1b*0\x1bN");
@@ -4923,0 +5004,8 @@ test "Screen vt with charsets" {
+    try testing.expectEqual(
+        t.screens.active.charset.charsets.get(.G2),
+        t2.screens.active.charset.charsets.get(.G2),
+    );
+    try testing.expectEqual(
+        t.screens.active.charset.single_shift,
+        t2.screens.active.charset.single_shift,
+    );
@@ -5073,0 +5162,82 @@ test "Terminal vt with tabstops" {
+test "Terminal vt restores cursor after margins and tabstops" {
+    const testing = std.testing;
+    const alloc = testing.allocator;
+
+    var builder: std.Io.Writer.Allocating = .init(alloc);
+    defer builder.deinit();
+
+    var t = try Terminal.init(alloc, .{
+        .cols = 20,
+        .rows = 6,
+    });
+    defer t.deinit(alloc);
+
+    var s = t.vtStream();
+    defer s.deinit();
+    s.nextSlice(
+        "\x1b[3g\x1b[6G\x1bH" ++
+            "\x1b[?69h\x1b[3;18s\x1b[2;5r\x1b[?6h\x1b[3;4H",
+    );
+
+    var formatter: TerminalFormatter = .init(&t, .vt);
+    formatter.content = .none;
+    formatter.extra = .all;
+    try formatter.format(&builder.writer);
+    const output = builder.writer.buffered();
+
+    var t2 = try Terminal.init(alloc, .{
+        .cols = 20,
+        .rows = 6,
+    });
+    defer t2.deinit(alloc);
+    var s2 = t2.vtStream();
+    defer s2.deinit();
+    s2.nextSlice(output);
+
+    try testing.expectEqual(t.scrolling_region, t2.scrolling_region);
+    try testing.expectEqual(t.modes.get(.origin), t2.modes.get(.origin));
+    try testing.expectEqual(
+        t.modes.get(.enable_left_and_right_margin),
+        t2.modes.get(.enable_left_and_right_margin),
+    );
+    try testing.expectEqual(t.tabstops.get(5), t2.tabstops.get(5));
+    try testing.expectEqual(t.screens.active.cursor.x, t2.screens.active.cursor.x);
+    try testing.expectEqual(t.screens.active.cursor.y, t2.screens.active.cursor.y);
+}
+
+test "Terminal vt restores DECCOLM before custom tabstops" {
+    const testing = std.testing;
+    const alloc = testing.allocator;
+
+    var builder: std.Io.Writer.Allocating = .init(alloc);
+    defer builder.deinit();
+
+    var source = try Terminal.init(alloc, .{ .cols = 80, .rows = 6 });
+    defer source.deinit(alloc);
+    var source_stream = source.vtStream();
+    defer source_stream.deinit();
+    source_stream.nextSlice("\x1b[?40h\x1b[?3h\x1b[3g\x1b[6G\x1bH\x1b[H");
+    try testing.expect(source.modes.get(.enable_mode_3));
+    try testing.expect(source.modes.get(.@"132_column"));
+    try testing.expectEqual(@as(size.CellCountInt, 132), source.cols);
+
+    var formatter: TerminalFormatter = .init(&source, .vt);
+    formatter.content = .none;
+    formatter.extra = .none;
+    formatter.extra.modes = true;
+    formatter.extra.tabstops = true;
+    try formatter.format(&builder.writer);
+
+    var replacement = try Terminal.init(alloc, .{ .cols = 80, .rows = 6 });
+    defer replacement.deinit(alloc);
+    var replacement_stream = replacement.vtStream();
+    defer replacement_stream.deinit();
+    replacement_stream.nextSlice(builder.writer.buffered());
+
+    try testing.expect(replacement.modes.get(.enable_mode_3));
+    try testing.expect(replacement.modes.get(.@"132_column"));
+    try testing.expectEqual(source.cols, replacement.cols);
+    try testing.expect(replacement.tabstops.get(5));
+    try testing.expect(!replacement.tabstops.get(8));
+}
+
