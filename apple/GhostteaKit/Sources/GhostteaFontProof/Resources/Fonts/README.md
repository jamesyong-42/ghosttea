# Bundled parity fonts

Run `npm run sync:fonts` from the repository root to populate this directory with the five
SHA-256-verified font files pinned by `native/fonts.lock.json`. Generated font binaries are ignored
by Git; the package includes the accompanying OFL license and notices from the pinned Ghostty tree.
